# homebrew-tap

Homebrew tap for [memkeeper](https://github.com/teflon07/memkeeper) — local-first memory for AI agents.

```sh
brew install teflon07/tap/memkeeper
memkeeper --version
```

After install, run `memkeeper pull-models` once to enable on-device semantic search (lexical BM25/FTS works without it).
